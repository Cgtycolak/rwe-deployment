# Core — security-critical; upper bounds prevent silent major-version breakage
# Flask 2.2.0 / Werkzeug 2.2.0 had CVE-2023-25577 (DoS) and CVE-2023-46136 (path traversal).
Flask>=3.0,<4
Flask-Session>=0.8.0
Flask-CORS>=4.0.0
Flask-SQLAlchemy>=3.0.0
Flask-Migrate>=4.0.0
Werkzeug>=3.0,<4
Jinja2>=3.1.4,<4
MarkupSafe>=2.1.5
itsdangerous>=2.2.0,<3      # session signing — major bumps can change signing contracts
gunicorn==21.2.0             # NEEDS-CONTEXT: gunicorn@21.2.0 — verify no open CVEs

# Data
# numpy <2.0 / pandas <2.0 constraints were stale; venv confirmed 2.x works.
numpy>=2.0.0
pandas>=2.0.0
python-dateutil>=2.9.0       # directly used (relativedelta); also pandas transitive
pytz>=2023.3                 # directly used throughout; provides Istanbul/UTC conversions
tzdata>=2024.2               # IANA timezone data, required by pytz on some platforms
# six removed: Python 2/3 compat shim, never imported in this Python 3 codebase

# Database
SQLAlchemy>=2.0.0,<3         # ORM for all queries — cap major version
psycopg2-binary>=2.9.9       # NOTE: bundles OpenSSL; system OpenSSL patches don't apply
alembic>=1.12.0              # migration engine (Flask-Migrate transitive)

# HTTP / networking — security-critical (all external API calls go through requests)
requests>=2.28.1,<3          # cap major version; TLS defaults can change across majors
certifi>=2024.0.0            # CA bundle for TLS — declared explicitly, update annually
charset-normalizer>=3.0.0    # requests transitive
idna>=3.0                    # requests transitive
urllib3>=2.0.0               # directly used (Retry class in functions.py, main.py)

# Scheduler
APScheduler>=3.10.4

# Visualisation & export
plotly>=5.19.0,<6            # NEEDS-CONTEXT: plotly@5.19.0 — chart JSON parsing surface
kaleido==0.2.1               # NEEDS-CONTEXT: kaleido@0.2.1 — last 0.2.x release (2021),
                             # verify subprocess starts in Render Python 3.11 container
openpyxl>=3.1.2              # Excel read/write .xlsx
xlrd>=2.0.1                  # Excel read .xls (old format uploads in forecasting routes)
xlsxwriter>=3.1.0            # Excel write via pd.ExcelWriter(engine='xlsxwriter')

# Forecasting support
holidays>=0.46               # Turkish public holiday flags in feature engineering
scikit-learn>=1.0.0          # confusion matrix in model_testing.py

# Scripts
tqdm>=4.60.0                 # used in app/scripts/cao_charts/populate_demand_data.py

# Env
python-dotenv>=1.0.1
